[phases.setup]
nixPkgs = [
  "nodejs_20",
  "chromium",
  "nss",
  "freetype",
  "harfbuzz",
  "ca-certificates",
  "fontconfig"
]

[phases.install]
cmds = ["npm install --prefix backend"]

[phases.build]
cmds = []

[start]
cmd = "node backend/server.js"
